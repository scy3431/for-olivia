This folder is a placeholder. Songs are streamed directly from YouTube via
the YouTube IFrame API (see the `playlist` array at the top of script.js),
so no audio files need to be placed here. Keep this folder only if you'd
like to store reference notes about each track.
